name: build-push
on:
  push:
    branches: [ai-portfolio]
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t ghcr.io/<you>/llm-gateway:latest ./gateway
      # TODO: login + push; add tests/scan
